{
  "name": "@base/jobs",
  "version": "0.0.1",
  "private": true,
  "description": "BullMQ NestJS module: configurable named queues, OTel metrics, BullBoard dashboard wiring.",
  "type": "module",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsgo -p tsconfig.json --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "lint": "oxlint src",
    "lint:fix": "oxlint --fix src",
    "format": "oxfmt \"src/**/*.ts\"",
    "format:check": "oxfmt --check \"src/**/*.ts\"",
    "check": "pnpm typecheck && pnpm lint && pnpm format:check",
    "clean": "tsc --build --clean && rm -rf dist coverage"
  },
  "dependencies": {
    "@base/cache": "workspace:*",
    "@bull-board/api": "7.0.0",
    "@bull-board/fastify": "7.0.0",
    "@opentelemetry/api": "1.9.1",
    "bullmq": "5.74.1"
  },
  "peerDependencies": {
    "@nestjs/common": "11.x",
    "@nestjs/config": "4.x",
    "@nestjs/platform-fastify": "11.x"
  }
}
