{
  "name": "@base/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Kafka consumer worker: drains tasks.events, enqueues a BullMQ job, processes it.",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/main.js",
    "start:dev": "NODE_OPTIONS='--conditions source --import tsx/esm' node src/main.ts",
    "typecheck": "tsgo -p tsconfig.json --noEmit",
    "lint": "oxlint src",
    "lint:fix": "oxlint --fix src",
    "format": "oxfmt \"src/**/*.ts\"",
    "format:check": "oxfmt --check \"src/**/*.ts\"",
    "check": "pnpm typecheck && pnpm lint && pnpm format:check",
    "clean": "rm -rf dist coverage"
  },
  "dependencies": {
    "@base/config": "workspace:*",
    "@base/jobs": "workspace:*",
    "@base/kafka": "workspace:*",
    "@base/logger": "workspace:*",
    "@base/observability": "workspace:*",
    "@confluentinc/kafka-javascript": "1.10.0",
    "@nestjs/common": "12.0.1",
    "@nestjs/config": "12.0.0",
    "@nestjs/core": "12.0.1",
    "@opentelemetry/api": "1.9.1",
    "@opentelemetry/instrumentation-nestjs-core": "0.67.0",
    "@opentelemetry/instrumentation-undici": "0.31.0",
    "bullmq": "6.3.2",
    "reflect-metadata": "0.2.2",
    "rxjs": "7.8.2"
  },
  "devDependencies": {
    "@types/node": "26.4.0",
    "tsx": "4.23.13",
    "typescript": "6.0.3"
  }
}
